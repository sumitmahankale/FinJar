[phases.setup]
nixPkgs = ['maven', 'openjdk11']

[phases.build]
cmds = [
    'cd backend',
    './mvnw clean package -DskipTests'
]

[phases.start]
cmd = 'cd backend && java -jar target/FinJar-0.0.1-SNAPSHOT.jar'

[variables]
MAVEN_OPTS = '-Xmx1024m'
